using System;

class Program
{
    static void Main()
    {
        Console.WriteLine("Enter multiple words, but separate them with commas:");
        string text = Console.ReadLine(); 
        
        string noSpacesText = text.Replace(" ", "");
        
        Console.WriteLine("String without spaces:");
        Console.WriteLine(noSpacesText);
    }
}
